/**
 * Legacy types file - Re-exports from new organized structure
 * 
 * @deprecated Use domain-specific imports instead:
 * - import { EntityDefinition } from './types/core'
 * - import { ViewOptions } from './types/views'  
 * - import { RLSCondition } from './types/permissions'
 */

// Re-export everything from new organized structure for backward compatibility
export * from './types/index';